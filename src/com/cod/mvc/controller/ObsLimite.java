package com.cod.mvc.controller;

import com.cod.mvc.Model.Coche;
import com.cod.mvc.Model.Model;

/**
 * Esta clase representa un observador en el patrón observador.
 */
public class ObsLimite implements Observer{
/**
     * Este método actualiza el observador con el coche y el modelo dados.
     * @param matricula El coche con el que actualizar el observador.
     * @param velocidad El modelo con el que actualizar el observador.
     */
    @Override
    public void update(String matricula, int velocidad) {

    }
}
